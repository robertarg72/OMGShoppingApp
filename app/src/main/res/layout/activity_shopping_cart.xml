<?xml version="1.0" encoding="utf-8"?>

<ScrollView android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:context="com.centennialcollege.omgshoppingapp.ShoppingCartActivity">

            <TextView
            android:id="@+id/greeting"
            style="@style/Greeting"
            />

            <ListView
                android:id="@+id/order_Items_list"
                android:layout_width="match_parent"
                android:layout_height="match_parent">
            </ListView>

            <Button
                android:id="@+id/checkoutButton"
                style="@style/checkoutButton"
                android:text="@string/checkout_button_text"
                android:onClick="onCheckoutButtonClick"/>

</LinearLayout>

</ScrollView>
